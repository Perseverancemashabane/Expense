{
  "name": "expense-tracker-root",
  "version": "1.0.0",
  "description": "Student Budget and Expense Tracker Root Orchestrator",
  "scripts": {
    "dev": "npm run dev --prefix student-budget-tracker",
    "build": "cd student-budget-tracker/client && npm install && npm run build",
    "seed": "npm run seed --prefix student-budget-tracker"
  },
  "dependencies": {
    "next": "^14.2.5",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
